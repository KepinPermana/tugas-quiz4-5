var nilai = [12,24, 36, 48, 60];
nilai.forEach(function (nilai) {
  console.log(nilai);
  // panggil fungsi bersama parameter
});
// ada metode lain yang bisa dipecahkan
var nilai =12;

for (let index = 0; index < 6; index++) {
  console.log(index*nilai);
}